{
  "private": true,
  "scripts": {
    "dev:web": "dx serve --web --open false",
    "dev:web:open": "dx serve --web",
    "dev:desktop": "dx serve --desktop",
    "tailwind:build": "tailwindcss -i ./input.css -o ./assets/tailwind.css --minify",
    "tailwind:watch": "tailwindcss -i ./input.css -o ./assets/tailwind.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "tailwindcss": "^4.1.0"
  }
}
